import { View, Text, StyleSheet } from "react-native";

export default function HomeScreen() {
    return (
        <View style={{ backgroundColor: '#000', flex:1, color:"#fff"}}>
            <Text>WaitPlaying</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    view: {
        backgroundColor: '#000',
        flex:1,
    },
    
})